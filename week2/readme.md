Week Number: [2]
Date: [23.12.2025]

✅ Completed This Week:
- Destructuring (objects and arrays)
- Spread operator (…) and rest parameters
- Template literals
- Array methods: map, filter, reduce, find
- Object methods and shorthand

[Exercise completed]
Exercise 1
    Working with an array of objects:
    filter selects only adult users (age ≥ 18).
    map gets their names.
    reduce calculates the average age of adults.
    → Practice using array methods for data processing.

Exercise 2
    Using object destructuring:
    The properties title, cpu, ram, price are extracted from the nested object product.
    A string with characteristics and price is formed.
    → Practice working with nested objects and template strings.

Exercise 3
    Combining settings:
    There are base settings (baseSettings) and user settings (userSettings).
    The unpacking operator ...